name: Deploy to Fly.io

on:
  push:
    branches: [main]
    paths:
      - 'Dockerfile'
      - 'fly.toml'
      - 'src/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/fly-deploy.yml'
  workflow_dispatch:

concurrency:
  group: fly-deploy
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy webclaw
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: superfly/flyctl-actions/setup-flyctl@master

      - run: flyctl deploy --remote-only --app webclaw
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Copy webshell to container
        run: flyctl ssh console --app webclaw -C "mkdir -p /app/dist/control-ui/webshell && cp /data/public/shell/index.html /app/dist/control-ui/webshell/index.html"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - run: flyctl status --app webclaw
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
